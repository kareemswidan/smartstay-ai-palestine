"use client";import{useEffect,useState}from"react";import{DashboardShell}from"@/components/dashboard-shell";import{useLanguage}from"@/components/providers";type User={id:number;full_name:string;email:string;role:string;created_at:string};export default function AdminUsers(){const{pick}=useLanguage();const[users,setUsers]=useState<User[]>([]);useEffect(()=>{fetch("/api/admin/overview").then(r=>r.json()).then(d=>setUsers(d.users||[])).catch(()=>{})},[]);return <DashboardShell type="admin"><div className="dashboard-heading"><div><span className="kicker">{pick("USER DIRECTORY","دليل المستخدمين")}</span><h1>{pick("Customers, hosts and admins","العملاء والمضيفون والإدارة")}</h1></div></div><section className="dashboard-panel recent-table"><div className="table-wrap"><table><thead><tr><th>{pick("Name","الاسم")}</th><th>{pick("Email","البريد")}</th><th>{pick("Role","الدور")}</th><th>{pick("Joined","تاريخ الانضمام")}</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td><span className="table-avatar">{user.full_name[0]}</span>{user.full_name}</td><td>{user.email}</td><td><span className={`status ${user.role}`}>{user.role}</span></td><td>{new Date(user.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></section></DashboardShell>}
