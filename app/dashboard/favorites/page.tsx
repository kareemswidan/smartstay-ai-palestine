"use client";import Link from"next/link";import{DashboardShell}from"@/components/dashboard-shell";import{PropertyCard}from"@/components/property-card";import{stays}from"@/lib/properties";import{useLanguage}from"@/components/providers";export default function Favorites(){const{pick}=useLanguage();return <DashboardShell type="customer"><div className="dashboard-heading"><div><span className="kicker">{pick("YOUR SHORTLIST","قائمتك المختصرة")}</span><h1>{pick("Saved stays","الإقامات المحفوظة")}</h1></div><Link href="/explore">{pick("Explore more","استكشف المزيد")} →</Link></div><div className="stays-grid dashboard-stays">{stays.slice(0,4).map(stay=><PropertyCard key={stay.id} stay={stay}/>)}</div></DashboardShell>}
