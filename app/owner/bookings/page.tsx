"use client";import{useEffect,useState}from"react";import{DashboardShell}from"@/components/dashboard-shell";import{useLanguage}from"@/components/providers";
type Row={id:number;booking_code:string;property_name:string;guest_name:string;guest_email:string;start_at:string;end_at:string;guests:number;total:number;status:string};
export default function OwnerBookings(){const{pick}=useLanguage();const[rows,setRows]=useState<Row[]>([]);useEffect(()=>{fetch("/api/bookings").then(r=>r.json()).then(d=>setRows(d.bookings||[])).catch(()=>{})},[]);return <DashboardShell type="owner"><div className="dashboard-heading"><div><span className="kicker">{pick("RESERVATION CONTROL","إدارة الحجوزات")}</span><h1>{pick("Bookings & availability","الحجوزات والتوفر")}</h1><p>{pick("Every confirmed time is automatically blocked from new reservations.","يتم حجب كل وقت مؤكد تلقائيًا من الحجوزات الجديدة.")}</p></div></div><section className="dashboard-panel recent-table"><div className="panel-head"><h3>{pick("All reservations","كل الحجوزات")}</h3><span className="live-indicator"><i/> {pick("Live calendar sync","مزامنة تقويم مباشرة")}</span></div><div className="table-wrap"><table><thead><tr><th>{pick("Reference","المرجع")}</th><th>{pick("Guest","الضيف")}</th><th>{pick("Property","المنشأة")}</th><th>{pick("Check-in / out","الدخول / الخروج")}</th><th>{pick("Guests","الضيوف")}</th><th>{pick("Total","الإجمالي")}</th><th>{pick("Status","الحالة")}</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><b>{row.booking_code}</b></td><td>{row.guest_name}<small className="table-sub">{row.guest_email}</small></td><td>{row.property_name}</td><td>{new Date(row.start_at).toLocaleString()}<small className="table-sub">→ {new Date(row.end_at).toLocaleString()}</small></td><td>{row.guests}</td><td><b>${row.total}</b></td><td><span className="status approved">{row.status}</span></td></tr>)}</tbody></table></div></section></DashboardShell>}
